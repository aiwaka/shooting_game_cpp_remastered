#pragma once

namespace utils {
    //! @brief val‚Ì¬”ˆÈ‰ºpointˆÊ‚ğlÌŒÜ“ü‚·‚é
    extern float round_point(float val, int point);
    extern float clamp(const float val, const float min, const float max);
}
